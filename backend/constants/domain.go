package constants

import "time"

const (
	Interval1s  = time.Second * 1
	Interval1m  = time.Minute * 1
	Interval5m  = time.Minute * 5
	Interval15m = time.Minute * 15
	Interval30m = time.Minute * 30
	Interval1h  = time.Hour * 1
	Interval4h  = time.Hour * 4
	Interval1d  = time.Hour * 24
)
